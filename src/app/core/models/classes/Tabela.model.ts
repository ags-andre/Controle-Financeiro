export interface Tabela {
  dados: any,
  colunas: string[][], //o indice 0 corresponde ao nome que aparecera na coluna da tabela, e o 1 corresponde ao mesmo nome, porem voltado para comparação com o atributo da classe que preencherá a tabela
  componente: string
}

